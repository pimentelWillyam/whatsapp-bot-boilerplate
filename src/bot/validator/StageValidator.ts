import { type Stage } from '../entity/Stage'
import { type IStageValidator } from '../interface/IStageValidator'

class StageValidator implements IStageValidator {
  validate (stage: Stage): void {
    
  }
}
